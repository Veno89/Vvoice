# Server Configuration
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# Security
# Generate a strong secret for production: openssl rand -hex 32
WEBRTC_DEV_JWT_SECRET=dev-secret-do-not-use-in-prod
JWT_SECRET=dev-secret-do-not-use-in-prod

# Database
DB_PATH=data/vvoice.db

# WebRTC / WebSocket Limits
MAX_ROOM_PARTICIPANTS=8
MAX_ROOMS_PER_CONNECTION=2
WS_MESSAGE_BURST=60
WS_MESSAGE_WINDOW_MS=10000

# CORS (comma separated)
CORS_ORIGINS=*

# TURN Server (Optional)
# TURN_HOST=
# TURN_PORT=3478
# TURN_SECRET=
