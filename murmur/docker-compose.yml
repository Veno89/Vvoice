services:
  murmur:
    image: mumblevoip/mumble-server:latest
    ports:
      - "64738:64738"
      - "64738:64738/udp"
    volumes:
      - ./data:/data
    environment:
      - MUMBLE_SUPERUSER_PASSWORD=dev_password
      - MUMBLE_ICE_READWRITE=true
    restart: unless-stopped
